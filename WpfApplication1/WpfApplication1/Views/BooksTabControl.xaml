<UserControl x:Class="WpfApplication1.Views.BooksTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
        <UserControl.DataContext>
            <Binding Path="BooksReadTabViewModel" Source="{StaticResource Locator}" />
        </UserControl.DataContext>
    <ListView ItemsSource="{Binding BooksRead}" ScrollViewer.VerticalScrollBarVisibility="Auto">
        <ListView.View>
            <GridView AllowsColumnReorder="false">
                <GridViewColumn Header="BookName"/>
            </GridView>
        </ListView.View>
    </ListView>
</UserControl>
